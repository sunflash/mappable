# Uncomment the next line to define a global platform for your project
platform :ios, '8.0'
use_frameworks!

target 'Mappable' do

    pod 'SwiftLint'
end

target 'MappableTests' do

    pod 'SwiftLint'
end